<h4>Client info</h4>
<?php if( $client->point_of_contact == "" && $client->phone_number == "" && $client->email == "" ): ?>
    <section class="info">
    <a title="Edit client" href="/clients/<?php echo $client->id; ?>/edit">
    <i class="fa fa-pencil-square-o fa-lg"></i> Edit</a> this contact for future reference.
    </section>
<?php else: ?>
    <ul class="list-style-none">
        <li><strong>Point of contact:</strong> <?php echo $client->point_of_contact; ?></li>
        <li><strong>Phone number:</strong> <?php echo $client->phone_number; ?></li>
        <li><strong>Email:</strong> <?php echo $client->email; ?></li>
    </ul>
<?php endif; ?>

<hr>
<h4>Client projects</h4>
<ul class="list-style-none">
    <?php if( count($projects) != 0): ?>
        <?php foreach($projects as $project): ?>
            <li><a href="/projects/<?php echo $project->id; ?>"><?php echo $project->name; ?></a></li>
        <?php endforeach; ?>
    <?php else: ?>
    <section class="info">
        <i class="fa fa-lightbulb-o"></i>
        This client has no projects at the moment, to create a project for
        this client use the for to the side.
    </section>
    <?php endif; ?>
</ul>

<hr>
<strong>Note:</strong>
<p class="dimmed">Deleting <i><?php echo $client->name; ?></i> will delete all projects and tasks associated with this client.</p>
<?php echo Form::open(array('action' => 'ClientsController@destroy', 'method' => 'delete')); ?>

    <input type="hidden" name="id" value="<?php echo $client->id; ?>">
    <input type="submit" class="btn btn-danger" value="Delete Client" />
<?php echo Form::close(); ?>