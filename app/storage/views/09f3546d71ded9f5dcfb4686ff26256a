<?php # /app/views/projects/index.blade.php  ?>


<?php $__env->startSection('content'); ?>

<div class="row main-row">
    <div class="col-xs-12">
        <div>
            <h2>Projects</h2>
        </div>        
    </div>

	<div class="col-xs-12">

        <?php /*Owner Projects*/ ?>
		<div class="app-wrapper">
            <div class="clearfix"></div>
            <h5>My Projects</h5>
            <?php if(count($projects) > 0 ): ?>
                <ul class="list-style-none">
                    <?php foreach($projects as $project): ?>
                        <?php $counter++; ?>
                        <a class="list-link"href="/projects/<?php echo $project->id; ?>">
                            <li><span class="numCount"><?php echo $counter; ?></span> <?php echo $project->name; ?></li>
                        </a>
                    <?php endforeach; ?>
                </ul>                
            <?php else: ?>
                <section class="info"><i class="fa fa-lightbulb-o">
                    </i> In order to create a project you must first create a <a href="/clients">client</a>
                </section>
            <?php endif; ?>
            <hr>
            <?php /*Member Projects*/ ?>
            <h5>Projects shared with me</h5>
            <?php if(count($inProjects) > 0 ): ?>
                <ul class="list-style-none">
                    <?php foreach($inProjects as $project): ?>
                        <?php $counter++; ?>
                        <a class="list-link"href="/projects/<?php echo $project->id; ?>">
                            <li>
                                <span class="numCount"><?php echo $counter; ?></span> <?php echo $project->name; ?>

                            </li>
                        </a>
                    <?php endforeach; ?>
                </ul>
            <?php else: ?>
                <section class="info" role="alert">
                    <i class="fa fa-lightbulb-o"></i>
                    No projects have been shared with you. In order to become a
                    member of a project a <i><b>project owner</b></i> has to send you an invite.
                </section>
            <?php endif; ?>
	   </div>
    </div>
</div>

<?php $__env->stopSection(); ?>



<?php echo $__env->make('templates/hud_master', array_except(get_defined_vars(), array('__data', '__path')))->render(); ?>